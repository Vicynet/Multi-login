spring:
  application:
    name: multi-login

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/multilogin
    username: secret
    password: 'secret'